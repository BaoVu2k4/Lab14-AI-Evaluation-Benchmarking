# Core
python-dotenv>=1.0.0
numpy>=1.26.0

# LLM providers (generation + judges)
groq>=0.11.0
openai>=1.10.0          # dùng cho Mistral & OpenRouter qua endpoint OpenAI-compatible

# RAG: embeddings + rerank + vector DB
cohere>=5.0.0
qdrant-client>=1.10.0

# Tiện ích
tqdm>=4.66.0
